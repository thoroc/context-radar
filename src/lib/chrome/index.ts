// Chrome domain: the site-wide top bar and the nav list it renders from.
// Consumed by the site-chrome Vite transform (for the three static entries) and
// by the tool-pages and markdown-pages generators.

export { NAV_LINKS } from "./nav-links";
export { renderTopbar } from "./render-topbar";
